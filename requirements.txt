# KB Server 依赖
# 核心
fastapi>=0.100
uvicorn>=0.23
numpy>=1.24

# 鉴权
python-jose>=3.3
passlib>=1.7
bcrypt>=4.0

# 文档解析
pymupdf>=1.23          # PDF
python-docx>=1.1       # Word
openpyxl>=3.1          # Excel
python-pptx>=0.6.23    # PPT

# OCR（扫描件/图片文字识别）
# 如不需要 OCR，可注释下面两行并设置环境变量 KB_OCR_ENABLED=0
paddleocr>=2.7
paddlepaddle>=2.5
（内容由AI生成，仅供参考）
